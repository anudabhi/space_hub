<% content_for :title, "Edit #{@listing.title} — SpaceHub" %>

<div class="container py-4" style="max-width:700px;">
  <h2 class="fw-bold mb-4">Edit Listing</h2>
  <%= render "form", listing: @listing %>
</div>
